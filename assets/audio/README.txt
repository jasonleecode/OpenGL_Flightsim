Sound effects go here:

- engine.wav  flight/engine sound, loops continuously, volume follows the throttle
- radio.wav   radio chatter, loops continuously at low volume

Both files are optional. Missing files only print a warning to the console.
After adding files, rebuild with: cmake --build build
